---
title: "Supported models: MusicGen, Stable Audio, MOSS-SoundEffect, Lyria, and Demucs"
description: "Compare MLX AudioGen's models — MusicGen, Stable Audio Open, MOSS-SoundEffect, Lyria, and Demucs — on specs, repo IDs, licensing, and which to pick."
---

MLX AudioGen runs four on-device model families plus one opt-in cloud model. **On-device:** MusicGen for autoregressive text-to-audio, Stable Audio Open for diffusion-based stereo generation, MOSS-SoundEffect for text-to-sound-effect generation, and Demucs v4 for stem separation — all running locally on your Mac's Metal GPU. **Cloud:** Lyria, Google's hosted music model on Vertex AI, is the first non-local model and is off by default. Each on-device family has multiple variants tuned for different output quality, speed, and conditioning capabilities. Pre-converted MLX weights for most on-device variants are published on [HuggingFace](https://huggingface.co/jasonvassallo) and download automatically on first use. The stereo-melody MusicGen variants are the exception — no converted weights are published for them, so convert those locally with `mlx-audiogen-convert`.

<Tip>
If you're using MLX AudioGen for the first time, start with a small model. The `musicgen-small` and `stable-audio-open-small` variants download and run fastest, and they produce good results for most prompts. Switch to larger variants once you're comfortable with the generation workflow.
</Tip>

## MusicGen

MusicGen is an autoregressive model from Meta that generates audio by predicting audio codec tokens from a text prompt. It encodes your prompt with T5, runs an autoregressive transformer with classifier-free guidance, then decodes tokens back to audio using EnCodec. All MusicGen variants output at 32 kHz.

### Variants

| Model | Size variants | Output | Conditioning |
|---|---|---|---|
| MusicGen | small, medium, large | Mono | Text |
| MusicGen Stereo | small, medium, large | Stereo | Text |
| MusicGen Melody | base, large | Mono | Text + melody audio |
| MusicGen Stereo Melody | base, large | Stereo | Text + melody audio |
| MusicGen Style | base | Mono | Text + style audio |

### HuggingFace repo IDs

Use these repo IDs with `mlx-audiogen-convert` to convert weights yourself, or they auto-resolve when using pre-built MLX weights:

| Variant | HuggingFace repo |
|---|---|
| MusicGen small | `facebook/musicgen-small` |
| MusicGen medium | `facebook/musicgen-medium` |
| MusicGen large | `facebook/musicgen-large` |
| MusicGen Stereo small | `facebook/musicgen-stereo-small` |
| MusicGen Stereo medium | `facebook/musicgen-stereo-medium` |
| MusicGen Stereo large | `facebook/musicgen-stereo-large` |
| MusicGen Melody base | `facebook/musicgen-melody` |
| MusicGen Melody large | `facebook/musicgen-melody-large` |
| MusicGen Stereo Melody base | `facebook/musicgen-stereo-melody` |
| MusicGen Stereo Melody large | `facebook/musicgen-stereo-melody-large` |
| MusicGen Style base | `facebook/musicgen-style` |

### Choosing a MusicGen variant

- **small** — fastest generation, lowest memory use, good for prototyping and short clips
- **medium** — balanced quality and speed, suitable for most production uses
- **large** — best quality, slower generation, highest memory use
- **stereo variants** — use 8 codebooks instead of 4 for left/right channel separation; useful when output will be played back on stereo systems
- **melody variants** — add `--melody my_audio.wav` to condition generation on the pitch contour of a reference file; works without `--melody` for text-only generation
- **style variants** — add `--style-audio reference.wav` to match the timbre and texture of a reference clip using MERT feature extraction; `--style-coef` controls how strongly the text prompt influences the output (default: 5.0)

### License and attribution

<Warning>
**MusicGen weights are licensed CC-BY-NC-4.0 — non-commercial use only.**

Meta splits the grant across two licenses. The *code* is MIT. The *weights* are [Creative Commons Attribution-NonCommercial 4.0](https://creativecommons.org/licenses/by-nc/4.0/). The MIT grant does not reach the weights, so you cannot rely on it to use them.

If your use is commercial, MusicGen's **weights** do not permit it. The MIT code itself carries no such restriction. Check [Licenses at a glance](#licenses-at-a-glance) before you choose a model — the terms differ substantially across this page.
</Warning>

The upstream model card states it plainly:

> Code is released under MIT, model weights are released under CC-BY-NC 4.0.

Summarized (the [license text](https://creativecommons.org/licenses/by-nc/4.0/legalcode) governs):

- Free to share and adapt for **non-commercial** purposes.
- You must give appropriate credit and indicate whether you made changes.
- **No commercial use** — including use primarily directed towards commercial advantage or monetary compensation.

Jade Copet, Felix Kreuk, Itai Gat, Tal Remez, David Kant, Gabriel Synnaeve, Yossi Adi, and Alexandre Défossez created MusicGen and published it in [Simple and Controllable Music Generation](https://arxiv.org/abs/2306.05284). Copyright © Meta Platforms, Inc. and affiliates.

#### Bundled components

Each `jasonvassallo/mlx-musicgen-*` mirror ships more than the MusicGen weights, and the extra pieces carry their own licenses:

| Component | Upstream | License | Shipped in |
|---|---|---|---|
| T5 text encoder | [`google-t5/t5-base`](https://huggingface.co/google-t5/t5-base) | [Apache-2.0](https://www.apache.org/licenses/LICENSE-2.0) | Every MusicGen mirror — full text at `LICENSE.t5-apache-2.0` |
| MERT-v1-95M feature extractor | [`m-a-p/MERT-v1-95M`](https://huggingface.co/m-a-p/MERT-v1-95M) | [CC-BY-NC-4.0](https://creativecommons.org/licenses/by-nc/4.0/legalcode) | `mlx-musicgen-style` only |

T5 is Apache-2.0 — a different grant from the CC-BY-NC-4.0 covering the weights beside it — so each mirror carries a separate copy of its license text rather than relying on the repository's own. MERT shares the mirror's CC-BY-NC-4.0, so the repository `LICENSE` already covers it.

<Note>
MLX AudioGen's own Apache-2.0 license covers **this project's code** — including the converter, the inference pipelines, the server, and the web UI. It grants you **no rights in any model's weights**. The mirrors published at `jasonvassallo/mlx-musicgen-*` are format-converted artifacts: weights remapped and re-serialized into MLX format for Apple silicon, with no retraining and no fine-tuning. The upstream CC-BY-NC-4.0 terms reach them unchanged, so each mirror ships its own `LICENSE`, `NOTICE`, and model card.
</Note>

## Stable Audio Open

Stable Audio Open is a diffusion-based model from Stability AI that generates stereo audio at 44.1 kHz. It encodes your prompt with T5, applies rectified flow ODE sampling through a Diffusion Transformer (DiT), then decodes with an Oobleck VAE. The diffusion approach lets you control generation quality vs. speed through the number of steps and CFG scale.

### Variants

| Variant | Sample rate | Output | Notes |
|---|---|---|---|
| small | 44.1 kHz | Stereo | **Gated** — accept the license on HuggingFace first |
| 1.0 | 44.1 kHz | Stereo | Larger DiT, dual time conditioning — gated model |

### HuggingFace repo IDs

| Variant | HuggingFace repo |
|---|---|
| Stable Audio Open small | `stabilityai/stable-audio-open-small` |
| Stable Audio Open 1.0 | `stabilityai/stable-audio-open-1.0` |

<Warning>
**Both** Stable Audio Open variants are gated, including the small one. Before you can download or convert either, accept the license agreement on its HuggingFace page ([small](https://huggingface.co/stabilityai/stable-audio-open-small), [1.0](https://huggingface.co/stabilityai/stable-audio-open-1.0)) and authenticate with `huggingface-cli login`. Without that you will get an "Access to model … is restricted" error rather than a download.
</Warning>

### License and attribution

**Powered by Stability AI**

Stable Audio Open is licensed under the [Stability AI Community License](https://huggingface.co/stabilityai/stable-audio-open-1.0/blob/main/LICENSE.md), not by MLX AudioGen's Apache-2.0 license — that covers this project's code, never the model weights.

> This Stability AI Model is licensed under the Stability AI Community License, Copyright ©  Stability AI Ltd. All Rights Reserved

Summarized (the agreement itself governs):

- Free for research and non-commercial use.
- Commercial use requires registration at [stability.ai/community-license](https://stability.ai/community-license).
- The grant **terminates above USD $1,000,000 annual revenue**.
- Stability AI's Acceptable Use Policy applies to generated output.
- You may not use the model or its outputs to create or improve another foundational generative AI model.

The MLX conversions published at `jasonvassallo/mlx-stable-audio*` are derivative works: weights converted to MLX format for Apple silicon, with no retraining and no change to weight values.

### Stable Audio parameters

Stable Audio uses different generation parameters than MusicGen:

| Parameter | Default | Description |
|---|---|---|
| `--steps` | 8 | Number of diffusion steps — more steps = higher quality, slower generation |
| `--cfg-scale` | 6.0 | CFG guidance scale — higher values follow the prompt more strictly |
| `--sampler` | `euler` | ODE sampler — `euler` or `rk4` |
| `--negative-prompt` | `""` | Describe what you don't want in the output |

## MOSS-SoundEffect

MOSS-SoundEffect is a text-to-sound-effect model from OpenMOSS. Instead of music, it generates sound effects — impacts, ambiences, foley, whooshes, and other non-musical audio — from a text prompt. It pairs a Qwen3-8B autoregressive language-model backbone with a 16-codebook RVQ audio tokenizer (delay-pattern decoding) and outputs mono audio at 24 kHz. The weights are quantized to 4-bit and licensed under Apache-2.0.

<Note>
MOSS-SoundEffect is available through the [web UI](/integrations/web-ui) and the [HTTP API](/api/generation) (`model: "moss_sfx"`). It is not exposed by the `mlx-audiogen` command-line tool, which generates with MusicGen and Stable Audio only.
</Note>

### Variants

| Variant | HuggingFace repo | Output | License |
|---|---|---|---|
| moss-sfx (4-bit) | `mlx-community/MOSS-SoundEffect-MLX-4bit` | 24 kHz mono | [Apache-2.0](https://www.apache.org/licenses/LICENSE-2.0) |

MOSS-SoundEffect runs through the [`mlx-speech`](https://pypi.org/project/mlx-speech/) package, which auto-resolves both the model weights and its companion `appautomaton/openmoss-audio-tokenizer-mlx` codec from the HuggingFace cache on first use. Because the weights resolve on demand, the model is always offered in the web UI's model selector even before a local copy has been downloaded.

### MOSS-SoundEffect parameters

MOSS-SoundEffect uses autoregressive sampling parameters. In the web UI, selecting the MOSS-SoundEffect model exposes these sliders:

| Parameter | Default | Range | Description |
|---|---|---|---|
| Temperature | 1.5 | 0.1–2.0 | Sampling temperature — higher values produce more varied output |
| Top K | 50 | 1–500 | Number of top-k token candidates considered at each step |
| Top P | 0.6 | 0.05–1.0 | Nucleus-sampling probability mass |
| Repetition Penalty | 1.2 | 1.0–2.0 | Penalizes repeated tokens to avoid looping artifacts |

<Note>
These defaults are tuned for MOSS-SoundEffect and differ from MusicGen's. The web UI seeds them automatically when you select the model. Over the HTTP API, `top_p` and `repetition_penalty` default to MOSS-appropriate values, and when you omit `temperature`/`top_k` the server substitutes the MOSS defaults (1.5 / 50) in place of the MusicGen schema defaults. See the [generation API reference](/api/generation) for the exact wire-format defaults.
</Note>

## Lyria (cloud, Google Vertex AI)

Lyria is Google's hosted music-generation model. You access it through Vertex AI. It is the **first and only cloud model** in MLX AudioGen. Unlike every other model on this page, it does **not** run on your Mac. When you generate with Lyria, MLX AudioGen sends your prompt to Google Cloud. Google's infrastructure runs the generation. You then receive the finished audio. Lyria is **opt-in and off by default**. Until you configure it, you never see it in the model selector or the API model list. The rest of MLX AudioGen stays fully local and offline.

<Warning>
**Lyria sends your prompt to Google Cloud.** Every other MLX AudioGen model runs entirely on-device with no data leaving your machine. Lyria is different — when you generate with it, your prompt text is transmitted to Google's Vertex AI and the audio is produced in the cloud. Only enable Lyria if you are comfortable with that egress. Generated audio is also **SynthID-watermarked** by Google (an inaudible watermark identifying it as AI-generated).
</Warning>

### Variants

| Model id | Status | Endpoint | Output |
|---|---|---|---|
| `lyria-002` | Stable | Vertex AI publisher `:predict` | ~30 s, 48 kHz stereo WAV |
| `lyria-3-pro-preview` | Preview | Vertex AI `/interactions` | Lyria 3 Pro — text input only |
| `lyria-3-clip-preview` | Preview | Vertex AI `/interactions` | Lyria 3 Clip — text input only |

<Note>
**`lyria-002` is Lyria 2 — the stable, verified model.** It returns a ~30-second 48 kHz stereo clip. You can also pass an optional negative prompt and seed. The **`lyria-3-*` models are Lyria 3 in preview** — treat them as experimental. They use a different (newer) Vertex AI endpoint. They accept only a text prompt, with no negative prompt or seed. For dependable results, prefer `lyria-002`.
</Note>

### Enabling Lyria

To use Lyria, you install the optional `[cloud]` extra and configure a Google Cloud project. **There is no API key.** You authenticate with Google [Application Default Credentials (ADC)](https://cloud.google.com/docs/authentication/application-default-credentials). ADC transparently uses whatever gcloud / Workload Identity Federation login you already have set up on the host.

1. Install the cloud extra:

   ```bash
   pip install "mlx-audiogen[cloud]"
   ```

2. Authenticate with Application Default Credentials (one of):

   ```bash
   # Local development — opens a browser login
   gcloud auth application-default login
   ```

   On a server you would instead use a service account or Workload Identity Federation; ADC picks up whichever is configured.

3. Point MLX AudioGen at your project (and, optionally, a region):

   ```bash
   export GOOGLE_CLOUD_PROJECT="my-gcp-project-id"
   export GOOGLE_CLOUD_LOCATION="us-central1"   # optional; defaults to us-central1
   ```

Once you set `GOOGLE_CLOUD_PROJECT`, you see the Lyria models in the web UI's model selector (badged as cloud) and in `GET /api/models`. When you leave it unset, MLX AudioGen does not offer Lyria, and nothing else changes. `GOOGLE_CLOUD_LOCATION` only affects the `lyria-002` `:predict` endpoint. The Lyria 3 preview endpoint always uses Google's `global` location.

<Note>
You run Lyria as an ordinary `/api/generate` job. It is a single blocking cloud round-trip (batch), not a streaming connection. Generate with it the same way you generate with a local model. See the [generation API reference](/api/generation) for the request fields (`negative_prompt` and `seed` apply to `lyria-002`).
</Note>

## Demucs v4

Demucs v4 (HTDemucs) is Meta's hybrid source separation model. It takes a stereo audio file and splits it into separate stems. Unlike the generation models, Demucs does not take a text prompt — it analyzes the audio signal directly.

### Variants

| Variant | Stems | Output | Description |
|---|---|---|---|
| `htdemucs` | 4 (drums, bass, vocals, other) | 44.1 kHz stereo | Standard separation |
| `htdemucs_6s` | 6 (drums, bass, vocals, other, guitar, piano) | 44.1 kHz stereo | Adds guitar and piano stems |

Demucs uses a hybrid architecture combining a spectral U-Net and a temporal U-Net connected by a CrossTransformer. Pre-converted MLX weights auto-download from [HuggingFace](https://huggingface.co/jasonvassallo/demucs-htdemucs-mlx) when you first run separation.

**License —** Meta releases Demucs under the **MIT** license. Its README states that plainly: "Demucs is released under the MIT license as found in the [LICENSE](https://github.com/facebookresearch/demucs/blob/main/LICENSE) file." MIT is permissive, and far less restrictive than [MusicGen](#musicgen), but it is not obligation-free — the copyright and permission notice must travel with copies, which is why the converted mirror ships the full license text rather than only a `license: mit` tag.

Simon Rouard, Francisco Massa, and Alexandre Défossez created HTDemucs v4 and published it in [Hybrid Transformers for Music Source Separation](https://arxiv.org/abs/2211.08553). Copyright © Meta Platforms, Inc. and affiliates.

<Note>
**Do your own diligence before commercial use.** Upstream states one MIT license for the project and publishes no separate terms for the released checkpoints — the license text itself grants rights in "the Software". Separately, the README records that `htdemucs` was trained on [MUSDB18-HQ](https://sigsep.github.io/datasets/musdb.html) plus 800 extra songs, and MUSDB18-HQ carries its own non-commercial terms. Whether training data terms reach model weights is unsettled, and Meta evidently takes the position that MIT covers what it ships. This page reports what upstream states rather than resolving that question for you.
</Note>

## Licenses at a glance

Licenses differ substantially between these models, and the difference matters most when your use is commercial. This table summarizes; the linked license text governs. Lyria is the exception — it distributes no weights, so no model license applies and your agreement with Google Cloud covers your use instead.

| Model | Upstream license | Commercial use |
|---|---|---|
| [MusicGen](#musicgen) — all variants | Weights [CC-BY-NC-4.0](https://creativecommons.org/licenses/by-nc/4.0/legalcode); code MIT | **No** for the weights — non-commercial only |
| [Stable Audio Open](#stable-audio-open) | [Stability AI Community License](https://huggingface.co/stabilityai/stable-audio-open-1.0/blob/main/LICENSE.md) | Registration required; the grant terminates above USD $1,000,000 annual revenue |
| [MOSS-SoundEffect](#moss-soundeffect) | [Apache-2.0](https://www.apache.org/licenses/LICENSE-2.0) | Yes |
| [Demucs v4](#demucs-v4) | Project released under [MIT](https://github.com/facebookresearch/demucs/blob/main/LICENSE); upstream publishes no checkpoint-specific terms | [Read the note first](#demucs-v4) — MIT is permissive, but nothing upstream states that it covers the released checkpoints |
| [Lyria](#lyria-cloud-google-vertex-ai) | Hosted service — no weights are distributed | Your Google Cloud agreement governs; output is SynthID-watermarked |

<Note>
MLX AudioGen is itself Apache-2.0, but that license covers **this project's code**. It grants you no rights in any model's weights — only each model's upstream license does that.
</Note>

## Picking the right model

<AccordionGroup>
  <Accordion title="I want to generate music from a text description">
    Start with **MusicGen small** for fast results. Use **MusicGen large** or **MusicGen Stereo large** when you need higher quality or stereo output for production use. Note that all MusicGen weights are **CC-BY-NC-4.0 — non-commercial only**; see [Licenses at a glance](#licenses-at-a-glance) if your use is commercial.
  </Accordion>
  <Accordion title="I want stereo output at CD quality (44.1 kHz)">
    Use **Stable Audio Open small** or **Stable Audio Open 1.0**. Stable Audio always outputs stereo at 44.1 kHz, compared to MusicGen's 32 kHz.
  </Accordion>
  <Accordion title="I want to guide generation with an existing melody or hummed idea">
    Use **MusicGen Melody** (base or large). Pass your reference audio with `--melody my_melody.wav`. The pipeline extracts the pitch contour from your file and uses it to condition generation alongside your text prompt.
  </Accordion>
  <Accordion title="I want to match the timbre or texture of a reference track">
    Use **MusicGen Style**. Pass your reference audio with `--style-audio reference.wav`. MERT extracts timbral features from the reference and blends them with your text prompt using dual classifier-free guidance.
  </Accordion>
  <Accordion title="I want to generate a sound effect, not music">
    Use **MOSS-SoundEffect**. Describe the sound — for example "heavy wooden door slam" or "distant thunder rumble" — and it generates a 24 kHz mono effect. It's available in the web UI and over the HTTP API (`model: "moss_sfx"`), not the CLI.
  </Accordion>
  <Accordion title="I want to split a mixed track into individual stems">
    Use **HTDemucs** for 4-stem separation (drums, bass, vocals, other), or **htdemucs_6s** if you also need guitar and piano isolated separately.
  </Accordion>
  <Accordion title="I want Google's Lyria model and I'm OK sending prompts to the cloud">
    Use **Lyria** (`lyria-002`). It runs on Google Vertex AI rather than on your Mac, so your prompt is sent to Google Cloud and the audio is SynthID-watermarked. Enable it with the `[cloud]` extra plus a `GOOGLE_CLOUD_PROJECT` and Application Default Credentials — no API key. The `lyria-3-*` variants are preview/experimental. See [Lyria](#lyria-cloud-google-vertex-ai) above.
  </Accordion>
</AccordionGroup>
